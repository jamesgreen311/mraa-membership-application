const exSheet = getSheet("Exhibitions")
const exSheetStruct = {
    applicantId: 0,
    content: 1
}

/**
 * 
 * @param {string} id 
 * @returns 
 */
function getApplicantExhibitions(id) {
    return content
}